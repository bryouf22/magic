%h1 New category

= form_for(@category, url: categories_path) do |form|
  .form-group
    %label Nom :
    = form.text_field(:name, class: 'form-control')
  = form.submit('Validate', class: 'btn btn-xs btn-success')
