%h3 Blocs
%table.table.bloc-table
  - @block_sets.each do |bloc|
    - sets = bloc.extension_sets.order('extension_sets.order ASC')
    %tr
      %td
        %table.table
          %tr
            %td{colspan: sets.count}=bloc.name
          %tr
            - sets.each do |set|
              %td.text-center{ class: "col-md-#{12/sets.count}"}
                = link_to extension_set_path(slug: set.slug) do
                  %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
                  %p= set.name


%h3 Editions de base
%table.table
  - @basic_editions.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Masterpieces
%table.table
  - @masterpieces.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 From the Vaults
%table.table
  - @from_the_vaults.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Duels decks
%table.table
  - @dual_decks.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Réédition / masters sets
%table.table
  - @reiditions.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Premium decks
%table.table
  - @premium_decks.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Houmouristiques
%table.table
  - @humouristics.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Débuter
%table.table
  - @starters.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Magic en ligne
%table.table
  - @onlines.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Autres
%table.table
  - @others.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name

%h3 Sans catégorie
%table.table
  - @_sets.each do |set|
    %tr
      %td.text-center
        = link_to extension_set_path(slug: set.slug) do
          %i{ class: "ss ss-common ss-6x ss-#{set.code&.downcase}" }
          %p= set.name
