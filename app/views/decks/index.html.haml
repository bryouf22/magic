%h1 Mes decks

= link_to new_deck_path, class: 'btn btn-xs btn-primary' do
  %span.glyphicon.glyphicon-plus
  Nouveau deck

%section.deck-list

- if @decks.any?
  %ul
    - @decks.each do |deck|
      %li
        = link_to deck.name, deck_path(slug: deck.slug)
        = link_to 'editer', edit_deck_path(slug: deck.slug)
- else
  %p Vous n'avez aucun deck
